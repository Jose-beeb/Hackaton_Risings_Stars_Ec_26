# Ojito al Mosquito — Dependencias del Backend
# Instalar con: pip install -r backend/requirements.txt

# === Framework web ===
fastapi>=0.110.0
uvicorn[standard]>=0.28.0

# === Validacion de datos ===
pydantic>=2.6.0
pydantic-settings>=2.2.0

# === HTTP client (Open-Meteo + requests async) ===
httpx>=0.27.0

# === Vision AI — Google Gemini ===
google-generativeai>=0.7.0

# === Variables de entorno ===
python-dotenv>=1.0.0

# === Soporte multipart (upload de imagenes) ===
python-multipart>=0.0.9

# === Testing ===
pytest>=8.0.0
